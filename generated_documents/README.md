# generated_documents/

Drop the final `.docx` you downloaded from AgileWriter here.

The **Check a generated document** panel in the web runner scans it for
placeholders that were never replaced — anything still looking like
`<Study Title>` means AgileWriter left it behind.
